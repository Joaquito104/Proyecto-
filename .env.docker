# Variables de entorno para Docker Compose
# IMPORTANTE: NO versionar este archivo con valores reales

# General
DEBUG=False
SECRET_KEY=CHANGE_THIS_TO_A_REAL_SECRET_KEY_AT_LEAST_50_CHARACTERS_LONG

# PostgreSQL
POSTGRES_DB=gestion_db
POSTGRES_USER=admin
POSTGRES_PASSWORD=secure_password_change_this
POSTGRES_PORT=5432

# Redis
REDIS_PASSWORD=redis_password_change_this
REDIS_PORT=6379

# MongoDB
MONGO_USER=admin
MONGO_PASSWORD=mongo_password_change_this
MONGO_PORT=27017

# URLs y hosts
FRONTEND_URL=http://localhost
ALLOWED_HOSTS=localhost,backend,127.0.0.1

# Puertos expuestos
BACKEND_PORT=8000
FRONTEND_PORT=80
